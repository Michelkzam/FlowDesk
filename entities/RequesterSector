{
  "name": "RequesterSector",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Nome do setor"
    },
    "description": {
      "type": "string",
      "description": "Descri\u00e7\u00e3o"
    },
    "status": {
      "type": "string",
      "enum": [
        "active",
        "inactive"
      ],
      "default": "active"
    }
  },
  "required": [
    "name"
  ]
}